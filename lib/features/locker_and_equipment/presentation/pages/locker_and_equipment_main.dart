import 'package:flutter/material.dart';

class LockerAndEquipmentMainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text(
        'LockerAndEquipmentMainPage',
        style: TextStyle(color: Colors.blue),
      )),
    );
  }
}
