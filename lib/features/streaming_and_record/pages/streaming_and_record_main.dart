import 'package:flutter/material.dart';

class StreamingAndRecordMainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text(
        'StreamingAndRecordMainPage',
        style: TextStyle(color: Colors.blue),
      )),
    );
  }
}
